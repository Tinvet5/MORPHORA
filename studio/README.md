# Studio source files

- `studio.js` contains the Content Studio data loading, OpenSeadragon editing, draft, validation, history, and export logic.
- `studio.css` contains the Studio-only branded layout and editor overlay styles.

The normal student-facing atlas remains in `index.html`, `script.js`, `navigation.js`, and `style.css`.
