# MORPHORA Deep Zoom assets

Each view folder contains a `.dzi` descriptor and a matching `<view-id>_files/` tile pyramid.

Do not rename, move, or partially upload a generated pyramid. The descriptor and every numbered level are required. Rebuild a view with `tools/build_deepzoom.py --overwrite` rather than editing tiles manually.
